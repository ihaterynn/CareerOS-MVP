[project]
name = "careeros-backend"
version = "0.1.0"
description = "Python FastAPI backend for CareerOS."
requires-python = ">=3.11"
dependencies = [
  "fastapi==0.115.6",
  "uvicorn[standard]==0.32.1",
  "pydantic-settings==2.7.0"
]

[tool.ruff]
line-length = 100
target-version = "py311"
